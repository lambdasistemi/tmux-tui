[toolchain]
channel = "1.90.0"
components = ["rustc", "cargo", "rust-src", "rustfmt", "clippy"]
# Static-musl targets for the release tarballs (built natively per arch).
targets = ["x86_64-unknown-linux-musl", "aarch64-unknown-linux-musl"]
profile = "minimal"
